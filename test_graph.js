const fs = require('fs');

async function test() {
    // Import WASM module via require since we are in node (we have to load it)
    // Wait, let's just write a cargo test instead to ensure it works.
}
